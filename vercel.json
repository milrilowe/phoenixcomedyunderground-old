{
  "framework": "astro",
  "buildCommand": "npx prisma generate && npm run build",
  "installCommand": "npm install && npx prisma generate",
  "functions": {
    "dist/server/pages/api/subscribe.astro.mjs": {
      "maxDuration": 10
    }
  },
  "env": {
    "PRISMA_CLI_BINARY_TARGETS": "rhel-openssl-1.0.x"
  }
}